const config: Record<string, unknown> = {
  appId: 'com.vtech.pro',
  appName: 'V-Tech PRO',
  webDir: 'out',
  server: {
    url: 'https://vtech-next.vercel.app',
    cleartext: true
  }
};

export default config;
